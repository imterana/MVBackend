language: python
python:
  - "3.6"

branches:
  only:
    - master

addons:
  apt:
    packages:
      - zip

install:
  - pip install boto3==1.3.0 awscli botocore pycodestyle

script:
  - pycodestyle .

deploy:
  provider: script
  script: bash deploy.sh
